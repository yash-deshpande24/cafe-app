# ===========================
# Application Info
# ===========================
spring.application.name=School-Management-System

# ===========================
# Server Configuration
# ===========================
# Bind to all network interfaces (so accessible via public IP)
server.address=0.0.0.0
server.port=8080

# ===========================
# Database Configuration (MySQL)
# ===========================
spring.datasource.url=jdbc:mysql://cafe.c5my6yoy8f9e.us-west-2.rds.amazonaws.com:3306/cafe?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=cafe_user
spring.datasource.password=Redhat123#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===========================
# Hibernate / JPA
# ===========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

